# Optional skills

This directory holds vendored and project-local skill catalogs that are not part of Brai runtime yet.

- `hermes-agent/` mirrors the official skill trees from `NousResearch/hermes-agent`.
- Refresh that mirror with `npm run skills:sync:hermes`.
